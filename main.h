#ifndef MAIN_H
#define MAIN_H
#include <stdarg.h>
#include <unistd.h>
int is_specifier(char c);
int _putchar(char c);
int _putstr(char *s);
int _strlen(char *s);
int _printf(const char *format, ...);
#endif
